export const metadata = {
  title: {
    default: "Cremson Publications",
    template: "%s | Cremson Publications",
  },
};

export default function CampaignLayout({ children }) {
  return children;
}
